<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/MainFragment"
    >

    <fragment
        android:id="@+id/MainFragment"
        android:name="com.hanafey.android.wol.MainFragment"
        android:label="@string/main_fragment_label"
        tools:layout="@layout/fragment_main"
        >
    </fragment>

    <fragment
        android:id="@+id/SettingsFragment"
        android:name="com.hanafey.android.wol.SettingsFragment"
        android:label="SettingsFragment"
        />

    <dialog
        android:id="@+id/HostStatusDialog"
        android:name="com.hanafey.android.wol.HostStatusDialog"
        android:label="HostStatusDialog"
        tools:layout="@layout/dialog_host_status"
        />

    <dialog
        android:id="@+id/WolStatusDialog"
        android:name="com.hanafey.android.wol.WolStatusDialog"
        android:label="WolStatusDialog"
        tools:layout="@layout/dialog_wol_status"
        />
</navigation>